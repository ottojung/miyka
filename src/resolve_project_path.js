/**
 * Resolves a project name to the file path of its project file.
 * @param {string} projectName
 * @returns {string}
 */
export function resolveProjectPath(projectName) {
    throw new Error('resolveProjectPath not implemented');
}
