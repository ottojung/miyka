/**
 * Interprets the given project file.
 * @param {string} projectFilePath
 * @returns {void}
 */
export function interpretProjectFile(projectFilePath) {
    // TODO: implement this.
    // the way it should work is it should `npm install` whatever is in `$(dirpath $projectFilePath)/package.json`.
    // then it should run that package as a command line script.
    throw new Error('interpretProjectFile not implemented');
}
